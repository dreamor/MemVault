---
description: Regenerate CLAUDE.md / AGENTS.md memory blocks from the MemVault store
---

Follow the memvault-sync skill now: run `memvault sync` against the current
project, report which rule files changed and what was written into them, and
remind me to commit them.
