# Tier 3 adapters

Tier 3 hosts have their own plugin systems but uncertain or limited
instruction-injection semantics. Qoder and Grok Build ship real manifests in
this repo (layouts verified by [ponytail](https://github.com/DietrichGebert/ponytail));
the rest register via the shared `skills/` + rules + MCP surface. Everything
shares the same memory store — set `MEMVAULT_AGENT_ID` per host.

## Qoder (closest to full tier 1)

- Rules: `.qoder/rules/memvault.md` is read per-project (canonical copy,
  regenerated by `scripts/gen-rule-copies.sh`). `AGENTS.md` also works.
- Manifest: `.qoder-plugin/plugin.json` (skills under `plugins/memvault/skills/`).
- MCP: register `memvault-mcp --transport stdio` in Qoder's MCP settings;
  identity `MEMVAULT_AGENT_ID=qoder` (see `mcp-clients/`).
- Hooks: Qoder's `UserPromptSubmit` hook template exists but no cross-host
  hook file is shipped yet — recall is rules-driven until batch 3.

## Grok Build

```
grok plugin install dreamor/memvault --trust
```

Reads the root `plugin.json` + `.grok-plugin/marketplace.json`; skills and
commands directories are resolved by manifest paths. Passive hook output
cannot inject instructions on Grok, so recall is rules-driven: point Grok at
`AGENTS.md` (repo root) or paste `integrations/rules/agents-snippet.md`.

## pi / Hermes / Devin / OpenClaw / Swival (manual, batch 3 candidates)

| Host | The short recipe |
|---|---|
| **pi** | `pi install git:github.com/dreamor/memvault` once a `pi-extension/` ships; until then register MCP + paste `integrations/rules/agents-snippet.md`. |
| **Hermes** | A Python plugin (pre-LLM-call injection + end-of-session extraction) would be full tier-1; tracked for batch 3. Meanwhile: MCP + AGENTS.md snippet. |
| **Devin** | Settings knowledge: paste `integrations/rules/agents-snippet.md`; run MemVault in HTTP mode (`memvault-mcp --transport http`) and register the MCP URL. |
| **OpenClaw** | `cp -r plugins/memvault/skills/memvault-* <project>/.openclaw/skills/` (each dir is a standard SKILL.md skill); MCP via OpenClaw config. |
| **Swival** | `cp -r plugins/memvault/skills/memvault-* <project>/.swival/skills/` or stage into the global library (`~/.config/swival/library`); repo-root AGENTS.md is read automatically once pasted. |

## MCP registries (manual submissions)

Prepared material lives in [`mcp-registry/`](mcp-registry/).
