Report on the MemVault integration for this machine: run `memvault status`
and `memvault doctor` (or `memvault-cli ...` if not on PATH), check whether
the MCP `memvault` server is reachable from this Codex session, and summarize
memory counts, pending review items, and any doctor findings as a short
table. Flag anything that would stop memories from flowing.
